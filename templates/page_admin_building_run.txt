<h1>
	<a href="/admin/">Admin</a>
	/
	<a href="/admin/buildings.php">Buildings</a>
	/
	<a href="/admin/building.php?id={$building.id}">{$building.name|escape}</a>
	/
	Preview
</h1>


<table cellpadding="0" cellspacing="0" border="0">
<tr valign="top">
<td style="padding-right: 80px;">

<h2>Custom Props</h2>

<form action="/admin/building_run.php?id={$building.id}" method="post">

<textarea name="custom" style="width: 300px; height: 150px;" wrap="virtual">{$smarty.post.custom|escape}</textarea><br />

<input type="submit" value="refresh" />

</form>

</td>
<td style="padding-right: 80px;">

<h2>Tileset</h2>

<div style="padding: 0px 20px 20px 20px; background-color: lime; width: {$base_w}px;">
<div style="position: relative; background-color: lime; width: {$base_w}px; height: {$base_h}px;">

	<!-- first comes the blue 'sky' masks -->

	{foreach from=$base_tiles item='tile'}
		<div style="position: absolute; left: {$tile.up_x}px; top: {$tile.up_y}px; width: {$tile.up_w}px; height: {$tile.up_h}px; background-color: blue;"></div>
	{/foreach}

	<!-- then the base tiles -->

	{foreach from=$base_tiles item='tile'}
		<div style="position: absolute; left: {$tile.xp}px; top: {$tile.yp}px;">
			<div style="position: absolute;"><img src="/images/large/red_base.gif" width="56" height="29" /></div>
{if 0}
			<div style="position: absolute; width: 56px; text-align: center">{$tile.x},{$tile.y}</div>
{/if}
		</div>
	{/foreach}

	<!-- and finally the actual tile pieces -->

	<div style="position: absolute; left: 0px; top: {$extra_height}px;">

		{map_draw_tileset tileset=$tileset.tiles}
	</div>

</div>
</div>

</td>
<td style="padding-right: 10px;">

<h2>Local Props</h2>

<table border="1">
{foreach from=$instance->props key='key' item='value'}
	<tr>
		<td><b>{$key|escape}</b></td>
{if $value|@is_array}
		<td>Array()</td>
{else}
		<td><code>{$value|escape}</code></td>
{/if}
	</tr>
{/foreach}
</table>

</td>
<td style="padding-right: 10px;">

<h2>Global Props</h2>

<table border="1">
{foreach from=$cfg.global_props.test key='key' item='value'}
	<tr>
		<td><b>{$key|escape}</b></td>
{if $value|@is_array}
		<td>Array()</td>
{else}
		<td><code>{$value|escape}</code></td>
{/if}
	</tr>
{/foreach}
</table>

</td>
</tr>
</table>

<p>The tileset should completely cover the red section, optionally extend into the blue and <b>not</b> touch the green.</p>

<p>This preview is created by running <code>on_design</code>, applying custom options, running <code>on_build</code>, applying custom options again, and finally running <code>on_refresh</code>.</p>

<p>Example custom line: <code>$i-&gt;set_local_prop('growth', 4);</code></p>



<pre>

</pre>
